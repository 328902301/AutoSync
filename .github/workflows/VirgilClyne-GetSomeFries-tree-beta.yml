name: VirgilClyne-GetSomeFries-tree-beta-sync
on:
  schedule:
    - cron: '45 */24 * * *'
  workflow_dispatch:
  watch:
    types: started
  repository_dispatch:
    types: sync-repo
jobs:
  repo-sync:
    env:
      PAT: ${{ secrets.PAT }} 
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: sync VirgilClyne-GetSomeFries-tree-beta
        uses: repo-sync/github-sync@v2
        if: env.PAT
        with:
          source_repo: "https://github.com/VirgilClyne/GetSomeFries.git"
          source_branch: "beta"
          destination_branch: "VirgilClyne-GetSomeFries-tree-beta"
          github_token: ${{ secrets.PAT }}