name: Cyberenchanter-scripts-sync
on:
  schedule:
    - cron: '35 */24 * * *'
  workflow_dispatch:
  watch:
    types: started
  repository_dispatch:
    types: sync-Cyberenchanter-scripts
jobs:
  repo-sync:
    env:
      PAT: ${{ secrets.PAT }} 
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: sync Cyberenchanter-scripts
        uses: repo-sync/github-sync@v2
        if: env.PAT
        with:
          source_repo: "https://github.com/Cyberenchanter/scripts.git"
          source_branch: "main"
          destination_branch: "Cyberenchanter-scripts"
          github_token: ${{ secrets.PAT }}