name: BlackJack8-iOSAdblockList-sync
on:
  schedule:
    - cron: '45 */24 * * *'
  workflow_dispatch:
  watch:
    types: started
  repository_dispatch:
    types: sync-repo
jobs:
  repo-sync:
    env:
      PAT: ${{ secrets.PAT }} 
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: sync BlackJack8-iOSAdblockList
        uses: repo-sync/github-sync@v2
        if: env.PAT
        with:
          source_repo: "https://github.com/BlackJack8/iOSAdblockList.git"
          source_branch: "master"
          destination_branch: "BlackJack8-iOSAdblockList"
          github_token: ${{ secrets.PAT }}